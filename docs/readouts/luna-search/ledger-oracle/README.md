# DCL public ledger oracle smoke

Pinned Catanatron image `58c092875440`, `PYTHONHASHSEED=0`. The harness checked every candidate decision against simulator hands in the test script only; production code reads only public hand totals.

See `result.json` for both one-game gates and `source.sha256` for the exact executed source hash.
